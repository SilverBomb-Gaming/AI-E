# Outputs
- Spawn/death audit JSON summarizing wave timing, health deltas, despawn hooks, and any missing FX.
- Screenshot or video proofs for key steps (spawn, aggro, death, cleanup) stored under `AI-E/ingest/media/...` as needed.
- Log bundle ready for CI ingestion so regressions link directly back to the failing probe.
- Recommendations for prefab or gameplay fixes when hooks are missing.
