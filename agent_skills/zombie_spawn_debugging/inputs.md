# Inputs
- Prefab reference (path + GUID) for the zombie under test.
- Scene or test route definition (DropZone configuration, probe scene GUID, NavMesh bake info).
- Spawn configuration manifest (spawn count, cadence, weapon/AI behaviors).
- Probe configuration file describing camera hooks, logging verbosity, and exit criteria.
- Baseline expectations (health on spawn, death fade timing, loot triggers).
